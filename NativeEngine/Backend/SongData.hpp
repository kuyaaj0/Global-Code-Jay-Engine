#pragma once

#include <string>

struct SongData
{
    std::string name;

    std::string folder;

    std::string chart;

    std::string instrumental;

    std::string voices;

    bool hasVoices;
};
